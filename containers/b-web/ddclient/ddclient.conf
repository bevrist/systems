# Configuration file for ddclient generated by debconf
#
# /etc/ddclient.conf
# TODO edit zone and target domain
# TODO set password to cloudflare token with `Zone-DNS-Edit` and `Zone-Zone-Read` permissions

protocol=cloudflare \
use=web, web=https://api.ipify.org/ \
login=token, \
password=<CLOUDFLARE_TOKEN> \
zone=brettevrist.net, \
home.brettevrist.net
